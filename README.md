# dotfiles

This repository holds my dot files setup.

To set them up:

```shell
$ make
```

Conflicting dotfiles will be saved to `backup`.

# Add new dotfiles

Simply call:

```shell
$ ./add.sh ~/.vimrc
```

